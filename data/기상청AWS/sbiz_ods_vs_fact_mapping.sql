-- ================================================================
-- ODS vs FACT 매핑 정보 (실행하지 말 것! 참고용 문서)
-- ================================================================
-- 
-- 이 파일은 tb_smb_ods → fact_smb_master 테이블 매핑 정보입니다.
-- SQL로 실행하지 마세요! 참고용 문서입니다.
--
-- [FACT 테이블 컬럼] → [ODS 테이블 컬럼] 매핑 관계:
-- 
-- seq_no (고유번호) → AUTO_INCREMENT
-- smb_id (상가업소번호) → col1 
-- smb_name (상호명) → col2 
-- smb_subnm (지점명) → col3 
-- cate1_cd (상권업종대분류코드) → col4 
-- cate1_nm (상권업종대분류명) → col5 
-- cate2_cd (상권업종중분류코드) → col6 
-- cate2_nm (상권업종중분류명) → col7 
-- cate3_cd (상권업종소분류코드) → col8 
-- cate3_nm (상권업종소분류명) → col9 
-- std_cd (표준산업분류코드) → col10
-- std_nm (표준산업분류명) → col11
-- addr1 (시도명) → col13
-- addr2 (시군구명) → col15
-- addr3 (행정동명) → col17
-- addr4 (법정동명) → col19
-- addr_num1 (지번본번지) → col23 [DECIMAL 변환]
-- addr_num2 (지번부번지) → col24 [DECIMAL 변환]
-- addr_bld (건물명) → col26
-- floor_nm (층 위치) → col36
-- lon (경도) → col38 [DECIMAL(16,12) 변환]
-- lat (위도) → col39 [DECIMAL(16,12) 변환]
--
-- ================================================================
-- 실행 순서:
-- 1. sbiz_create_fact_smb_master.sql (FACT 테이블 생성)
-- 2. sbiz_insert_fact_smb_master.sql (데이터 이관)
-- ================================================================